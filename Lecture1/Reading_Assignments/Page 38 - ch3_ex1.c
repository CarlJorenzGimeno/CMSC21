#include <stdio.h>

int main (void) {
    //Declaring variables
    int i, j;
    float x, y;

    //Assigning values to variables
    i = 10;
    j = 20;
    x = 43.2892f;
    y = 5527.0f;

    //Print output and pass variables as arguments to %d and %f
    printf("i = %d, j = %d, x = %f, y = %f\n", i, j, x, y);
}
