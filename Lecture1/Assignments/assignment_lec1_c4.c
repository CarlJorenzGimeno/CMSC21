#include <stdio.h>

int main(Void){                            //Added an opening brace
    int sum;                               //Changed INT into int
    //COMPUTE RESULT                         Changed into a one line comment
    sum = 25 + 37 - 19;                    //Added semicolon
    // DISPLAY RESULTS                       Changed into a one line comment and removed redundant slashes
    printf ("The answer is %i\n",sum);     //Added a comma between string and variable
    return 0;
}
